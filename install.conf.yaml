- clean:
    ~/:

- link:
    ~/.config:
    ~/.nixpkgs:
    ~/.vimrc:
    ~/.zsh_history:
      force: true
    ~/.zshenv:
    ~/.zshrc:

- shell:
    - chmod 700 ~/.config/op/
    - chmod 600 ~/.config/op/config
    - chmod 700 ~/.config/op/plugins/used_items/
